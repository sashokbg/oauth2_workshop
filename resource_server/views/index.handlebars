Resource Server running on port 3001.

<p>Protected endpoints:</p>
<ul>
  <li><code>GET /agenda</code> — requires <code>Authorization: Bearer &lt;token&gt;</code> with scope <code>agenda.read</code></li>
</ul>
